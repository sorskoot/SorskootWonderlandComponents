/**
 * Test if a bit is set in a flag
 * @param flag the flag
 * @param bit the bit to test
 * @returns true if the bit is set, false otherwise
 */
export declare function flagBitIsSet(flag: number, bit: number): boolean;
export declare function formatNumber(n: number): string;
export declare function timeSinceEpoch(): number;
export declare function moveLastToFirst<T>(arr: T[]): T[];
export declare function moveFirstToLast<T>(arr: T[]): T[];
